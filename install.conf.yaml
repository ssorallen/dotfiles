- link:
    ~/.bash: .bash/
    ~/.bash_profile: .bash_profile
    ~/.bashrc: .bashrc
    ~/.vimrc: .vimrc
- shell:
    - ["git submodule update --init --recursive", "Installing/updating submodules"]
